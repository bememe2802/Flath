server:
  port: 8087
  servlet:
    context-path: /newsfeed

spring:
  application:
    name: newsfeed-service
  data:
    redis:
      host: localhost
      port: 6379

app:
  services:
    post:
      url: http://localhost:8083/post
    profile:
      url: http://localhost:8081/profile
